import styled from "styled-components";

const APYChartWrapper = styled.div`
  border-radius: var(--border-radius);
  padding: var(--padding);
  box-shadow: var(--box-shadow);
  background-color: white;
`;

export { APYChartWrapper };
