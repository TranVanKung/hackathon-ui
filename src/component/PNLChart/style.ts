import styled from "styled-components";

const PNLChartWrapper = styled.div`
  border-radius: var(--border-radius);
  padding: var(--padding);
  box-shadow: var(--box-shadow);
  background-color: white;
`;

export { PNLChartWrapper };
