import styled from "styled-components";

const DailyPNLChartWrapper = styled.div`
  width: 100%;
  border-radius: var(--border-radius);
  padding: var(--padding);
  box-shadow: var(--box-shadow);
  background-color: white;
`;

export { DailyPNLChartWrapper };
